const express = require("express");
const router = express.Router();
const orderController = require("../controllers/orderController");



/*
 * Aqui me tranqué, si alguien tiene idea como hacerlo es bienvenido, si no igual estoy investigando
 * se deben crear los 5 endpoints, que corresponden al CRUD estándar (Create-Read-Update-Delete).
 * luego definir los prefijos en `routes/index.js`.
 *
 * 
 */